<%# Content Heading %>
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
} %>

<%# Message Block %>
<%- messages() %>

<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
<% } %>

<div class="alignCenter">
    <div id="loginFormContainer">
        <form action="/inv/management/inventory" method="post">
            <div><span>Make*:</span><input type="text" name="inv_make" id="invMake" value="<%= locals.inv_make %>" required></div>
            <div><span>Model*:</span><input type="text" name="inv_model" id="invModel" value="<%= locals.inv_model %>" required></div>
            <div><span>Year*:</span><input type="number" name="inv_year" id="invYear" min="1900" max="2023" value="<%= locals.inv_year %>" required></div>
            <div><span>Description*:</span><input type="text" name="inv_description" id="invDescription" value="<%= locals.inv_description %>" required></div>
            <div><span>Image*:</span><input type="text" name="inv_image" id="invImage" value="<%= locals.inv_image %>" pattern="[a-zA-Z0-9./_-]+" placeholder="/images/vehicles/no-image.png" required></div>
            <div><span>Thumbnail Image*:</span><input type="text" name="inv_thumbnail" id="invThumbnail" value="<%= locals.inv_thumbnail %>" pattern="[a-zA-Z0-9./_-]+" placeholder="/images/vehicles/no-image-tn.png" required></div>
            <div><span>Price*:</span><input type="number" name="inv_price" id="invPrice" value="<%= locals.inv_price %>" required></div>
            <div><span>Miles*:</span><input type="number" name="inv_miles" id="invMiles" value="<%= locals.inv_miles %>" required></div>
            <div><span>Color*:</span><input type="text" name="inv_color" id="invColor" value="<%= locals.inv_color %>" pattern="[a-zA-Z]+" required></div>
            <div><span>Classification*:</span><select name="classification_id" id="classificationId" value="<%= locals.classification_id %>" required><%- classificationOptions %></select></div>
            <div class="buttonContainer"><input type="submit" value="Add Inventory"></div>
        </form>
        <div class="fineprint">* Denotes a required field.</div>
        
    </div>
</div>